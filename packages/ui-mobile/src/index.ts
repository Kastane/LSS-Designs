/**
 * Hybrid mobile adapter (Capacitor/Cordova).
 *
 * Since Capacitor/Cordova render the app in a WebView, we reuse `@lss/ui-web`.
 * Keep mobile-specific wrappers here (safe areas, mobile nav patterns, etc).
 */

export * from '@lss/ui-web'


