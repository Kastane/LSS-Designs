# `@lss/ui-desktop`

Desktop UI adapter for **Electron/Tauri**.

Options:

- We reuse `@lss/ui-web` as-is and only add wrappers here (menus, window chrome, shortcuts).
- If we ever move to a native desktop renderer, we can revisit the architecture.


