/**
 * Desktop UI kit placeholder.
 *
 * With Electron/Tauri + React, we reuse `@lss/ui-web`.
 * Keep desktop-specific wrappers here (window chrome, menus, shortcuts, etc).
 */

export * from '@lss/ui-web'


