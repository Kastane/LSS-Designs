/**
 * Platform-agnostic primitives live here.
 *
 * Today it's intentionally minimal — we'll grow it as we unify tokens, types,
 * and behavioral primitives between web / mobile / desktop packages.
 */

export type { PropsWithClassName } from './types'


